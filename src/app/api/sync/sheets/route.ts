import { NextResponse } from "next/server";

/**
 * API de sincronização com Google Sheets
 * Placeholder para garantir build e deploy
 */
export async function GET() {
  return NextResponse.json({
    status: "ok",
    message: "Rota de sincronização ativa",
  });
}
